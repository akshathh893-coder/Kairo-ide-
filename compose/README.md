# Docker Compose Stacks

Reference Compose files for the KAIRO runtime environment.

These are used by the `local` provider and as the base for cloud providers that run Docker.

Key services (planned):
- code-server / openvscode-server
- Caddy or Traefik (reverse proxy + automatic TLS when needed)
- Monitoring agents
- Optional language-specific sidecars
