# Bootstrap Playbook

When asked to improve or extend the bootstrap process:

1. Read `install.sh` and `doctor.sh`.
2. Read any related specification under `docs/specifications/`.
3. Keep the scripts idempotent.
4. Prefer clear, actionable error messages.
5. Do not introduce new external dependencies without updating the doctor checks and documentation.
